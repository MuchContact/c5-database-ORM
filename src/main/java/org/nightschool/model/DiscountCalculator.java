package org.nightschool.model;

/**
 * Created by Administrator on 15-1-9.
 */
public class DiscountCalculator {
//    public double getDiscountedTotalPrice(Disk disk) {
//        //TODO Refactor with interface
//        if (disk.getName().equals("小清新光盘")) {
//            int discountNumber = disk.getNumber() / 3;
//            return disk.getTotalPrice() - disk.getPrice() * discountNumber;
//        }
//        if (disk.getName().equals("婚庆光盘")) {
//            int discountNumber = (int) (disk.getTotalPrice() / 100);
//            return disk.getTotalPrice() - discountNumber * 10;
//        }
//        if(disk.getName().equals("1TB大容量光盘")){
//            return disk.getTotalPrice() * 0.95;
//        }
//        return disk.getTotalPrice();
//    }
}
